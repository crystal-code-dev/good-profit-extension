import ReactDOM from "react-dom/client";

import { PopupContainer } from "@containers/pop-up";

ReactDOM.createRoot(
  document.getElementById("good-profit-extension-root")!
).render(<PopupContainer />);
